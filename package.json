{
  "name": "mednavigator-frontend-deploy",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm --prefix frontend ci",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start"
  },
  "engines": {
    "node": ">=20"
  }
}
