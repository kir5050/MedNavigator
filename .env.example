# OpenRouter (OpenAI-compatible API)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=anthropic/claude-opus-4.6

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=["https://app.navimedic.ru", "https://navimedic.ru"]

# Application
APP_ENV=development
LOG_LEVEL=info

# Voice input feature flag — off by default.
# Frontend has its own flag: VITE_VOICE_INPUT_ENABLED (frontend/.env.local).
VOICE_INPUT_ENABLED=false

# Analytics dashboard access
ADMIN_TOKEN=change-me-in-production

# Telegram error alerts
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Session expiry (hours)
SESSION_TTL_HOURS=24

# LLM cache TTL (seconds)
CACHE_TTL_CLARIFICATION=86400
CACHE_TTL_TRIAGE=604800
